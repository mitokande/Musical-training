; PHP-FPM per-directory overrides (read from the served docroot).
; Raises the 2M default so profile cover / media uploads go through.
; NOTE: nginx client_max_body_size must also allow this (vhost config).
upload_max_filesize = 10M
post_max_size = 12M
